apply plugin: 'groovy'

repositories {
    mavenCentral()
}
sourceSets {
    main {
        groovy.srcDirs = [file('src'), file('vars')]
    }
    test {
        groovy.srcDirs = [file('test')]
    }
}

test {
    include "test"
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:2.4.5'
    testCompile 'junit:junit:4.12'
    testCompile group:'com.lesfurets', name:'jenkins-pipeline-unit', version: '+'

}